import React, { useState } from 'react';

const Series = () => {
    const [isOpen, setIsOpen] = useState(false);

    return (
        <div id="series-content" className="section-content">
            {/* Your series content here */}
        </div>
    );
};

export default Series;